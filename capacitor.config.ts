import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.discipline.master',
  appName: '7 Days of Discipline',
  webDir: 'dist'
};

export default config;
